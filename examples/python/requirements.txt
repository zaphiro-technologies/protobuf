# Note: This project uses Poetry for dependency management.
# For better dependency resolution, use: poetry install
# This requirements.txt is kept for backward compatibility.
rstream>=0.15.0,<1.0.0
pika>=1.3.0,<2.0.0